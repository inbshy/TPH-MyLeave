# Copy these lines into this file (or use test.env for local overrides on desktop).
# No spaces after =. Do not commit real keys to git.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
